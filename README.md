# Flutter Time table app

Simple time table app

## Getting Started

This project is a Flutter application.

### Prerequisites
- Flutter (version)
- Dart (version)

### Installation
1. Clone the repository
2. Run `flutter pub get`
3. Run `flutter run`